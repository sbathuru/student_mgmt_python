SQLITE_DB_PATH=student.db

# Optional Oracle configuration. If these are set, the app uses Oracle instead of SQLite.
# ORACLE_USER=system
# ORACLE_PASSWORD=your_password
# ORACLE_DSN=localhost/XEPDB1
